version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres-vms
    restart: always
    environment:
      POSTGRES_DB: vmsdb-dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - vms-network

volumes:
  postgres_data:

networks:
  vms-network: